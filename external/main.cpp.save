#include <stdio.h>
#include <wiringPi.h>

#define DELAY 13

void pulse(int length){
	while(length >= 0){
		
		(DELAY);
		
	}
}

int main(){
	wiringPiSetup();
	pinMode(1, OUTPUT);

	printf("Hello");
	return 0;
}
